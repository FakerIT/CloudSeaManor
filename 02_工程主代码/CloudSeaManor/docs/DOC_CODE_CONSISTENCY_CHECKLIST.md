# 文档-代码一致性审计清单

## 改动规则
- 修改设计意图时，必须同步更新对应文档与 `TASKS.md`
- 新增系统入口时，代码头文件注明设计文档路径
- 每季度执行一次文档漂移审计

## 审计步骤
1. 从 `GameRuntime`、`GameWorldSystems`、`Data` 加载入口抽样 10 个关键点
2. 检查是否能追溯到设计文档与任务条目
3. 标记漂移：实现已变更但文档未更新
4. 产出本季度审计结论与修复计划

## 审计频率
- 固定在每季度最后一个迭代周
- 结果写入迭代复盘文档并挂到下一期任务

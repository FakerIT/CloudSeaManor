# 新系统接入游戏主循环 - 集成报告

> **项目**: 云海山庄 (Cloud Sea Manor)
> **日期**: 2026-05-02
> **状态**: ✅ 已完成

---

## 一、集成概述

本次集成将以下三个新系统接入到游戏主循环：
1. **钓鱼系统** (FishingSystem + FishingUI)
2. **观云镜系统** (MysticMirrorSystem + MysticMirrorUI)
3. **茶灵契约系统** (ContractSystem + ContractUI)

---

## 二、修改的文件

### 2.1 头文件修改

| 文件 | 修改内容 |
|------|---------|
| `GameRuntime.hpp` | 添加新系统头文件引用 |
| `GameApp.hpp` | 添加 `TryStartFishing_()` 方法声明 |

### 2.2 源文件修改

| 文件 | 修改内容 |
|------|---------|
| `GameRuntime.cpp` | 初始化新系统 + 日切刷新 |
| `GameApp.cpp` | 输入处理 + Update + Render + 辅助函数 |

### 2.3 新增文件

| 文件 | 用途 |
|------|------|
| `NewSystemsIntegration.hpp` | 新系统快速访问接口 |
| `PixelMysticMirrorPanel.hpp` | 观云镜UI头文件 |
| `【集成指南】_新系统接入游戏主循环.md` | 集成文档 |
| `【集成代码】_快速集成代码块.md` | 快速代码块 |

---

## 三、具体修改详情

### 3.1 GameRuntime.hpp

添加新系统头文件：

```cpp
// ============================================================================
// 新系统头文件
// ============================================================================
#include "CloudSeamanor/FishingSystem.hpp"
#include "CloudSeamanor/MysticMirrorSystem.hpp"
#include "CloudSeamanor/ContractSystem.hpp"
```

### 3.2 GameRuntime.cpp

#### 初始化（约第1555行）

```cpp
// ============================================================================
// 新系统初始化（钓鱼系统、观云镜系统、契约系统）
// ============================================================================
domain::FishingSystem::Instance().Initialize();
domain::MysticMirrorSystem::Instance().Initialize();
domain::ContractSystem::Instance().Initialize();
```

#### 日切刷新（约第2025行）

```cpp
// ============================================================================
// 新系统日切刷新
// ============================================================================
domain::MysticMirrorSystem::Instance().RefreshDaily();
```

### 3.3 GameApp.hpp

添加辅助函数声明：

```cpp
// ============================================================================
// 新系统辅助函数
// ============================================================================
void TryStartFishing_();
```

### 3.4 GameApp.cpp

#### 头文件引用（约第1行）

```cpp
// ============================================================================
// 新系统头文件
// ============================================================================
#include "CloudSeamanor/FishingSystem.hpp"
#include "CloudSeamanor/engine/FishingMiniGame.hpp"
#include "CloudSeamanor/MysticMirrorSystem.hpp"
#include "CloudSeamanor/engine/PixelMysticMirrorPanel.hpp"
#include "CloudSeamanor/ContractSystem.hpp"
#include "CloudSeamanor/engine/PixelContractPanel.hpp"
```

#### 输入处理（约第757行）

```cpp
// ============================================================================
// 新系统UI输入处理
// ============================================================================
// 钓鱼小游戏输入（最高优先级）
if (FishingUI::Instance().IsFishing()) {
    FishingUI::Instance().HandleInput(event);
    continue;
}

// 观云镜UI输入
if (MysticMirrorUI::Instance().IsVisible()) {
    if (MysticMirrorUI::Instance().HandleInput(event)) {
        continue;
    }
}

// 契约UI输入
if (ContractUI::Instance().IsVisible()) {
    if (ContractUI::Instance().HandleInput(event)) {
        continue;
    }
}
```

#### 快捷键（约第855行）

```cpp
// ============================================================================
// 新系统快捷键
// ============================================================================
else if (key->scancode == sf::Keyboard::Scancode::F) {
    // 钓鱼（按F键）
    TryStartFishing_();
} else if (key->scancode == sf::Keyboard::Scancode::G) {
    // 观云镜（按G键）
    if (!MysticMirrorUI::Instance().IsVisible()) {
        MysticMirrorUI::Instance().Show();
        SetHintMessage(runtime_.WorldState(), "打开观云镜", 1.5f);
    }
} else if (key->scancode == sf::Keyboard::Scancode::Y) {
    // 茶灵契约（按Y键）
    if (!ContractUI::Instance().IsVisible()) {
        ContractUI::Instance().Show();
        SetHintMessage(runtime_.WorldState(), "打开茶灵契约", 1.5f);
    }
}
```

#### Update（约第885行）

```cpp
// ============================================================================
// 新系统UI更新
// ============================================================================
FishingUI::Instance().Update(delta_seconds);
MysticMirrorUI::Instance().Update(delta_seconds);
ContractUI::Instance().Update(delta_seconds);
```

#### Render（约第1375行）

```cpp
// ============================================================================
// 新系统UI渲染
// ============================================================================
FishingUI::Instance().Render(window);
MysticMirrorUI::Instance().Render(window);
ContractUI::Instance().Render(window);
```

#### 辅助函数 TryStartFishing_()

```cpp
void GameApp::TryStartFishing_() {
    // 获取当前地图ID
    const std::string& map_id = runtime_.WorldState().GetCurrentMapId();

    // 确定钓鱼地点
    std::string location_id = "main_farm";
    if (map_id.find("river") != std::string::npos) {
        location_id = "river";
    } else if (map_id.find("spirit_realm") != std::string::npos) {
        location_id = "spirit_realm";
    }

    // 检查玩家是否在水边（简化检测）
    SetHintMessage(runtime_.WorldState(), "开始钓鱼！按 A/D 键保持位置", 2.0f);

    // 计算难度
    auto& fishing_system = domain::FishingSystem::Instance();
    int difficulty = fishing_system.CalculateFinalDifficulty(1, location_id);

    // 获取钓鱼技能等级
    int fishing_level = 20; // TODO: 从玩家属性获取

    // 开始钓鱼
    auto& fishing_ui = FishingUI::Instance();
    fishing_ui.StartFishing(difficulty);

    // 设置回调
    fishing_ui.SetOnCatchCallback([this](bool success, const std::string& fish_id) {
        if (success && !fish_id.empty()) {
            // 将鱼添加到背包
            runtime_.WorldState().MutablePlayer().AddToInventory(fish_id, 1);

            // 获取鱼名
            const auto* fish_data = domain::FishingSystem::Instance().GetFishData(fish_id);
            std::string fish_name = fish_data ? fish_data->name_zh : fish_id;

            // 显示提示
            SetHintMessage(runtime_.WorldState(), "钓到了 " + fish_name + "！", 3.0f);
        } else {
            SetHintMessage(runtime_.WorldState(), "狡猾的鱼溜走了...", 2.0f);
        }
    });
}
```

---

## 四、快捷键映射

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| F | 钓鱼 | 在水边按F开始钓鱼小游戏 |
| G | 观云镜 | 打开每日运势面板 |
| Y | 契约 | 打开茶灵契约面板 |
| A/D | 钓鱼移动 | 钓鱼小游戏时控制进度条 |
| ESC | 关闭UI | 关闭观云镜/契约面板 |

---

## 五、后续工作

### 5.1 待完成项

1. **存档集成** - 新系统需要与存档系统集成
   - 钓鱼技能/统计数据存档
   - 观云镜查看历史存档
   - 契约进度存档

2. **玩家属性集成** - 钓鱼技能等级应从玩家属性获取

3. **钓鱼点检测** - 完善玩家是否在水边的检测逻辑

4. **音频反馈** - 添加音效
   - 钓鱼成功音效
   - 观云镜打开音效
   - 契约完成音效

5. **物品sprite** - 添加钓鱼和契约物品的视觉资源

### 5.2 建议的后续任务

- [ ] 实现新系统存档读写
- [ ] 完善钓鱼点检测逻辑
- [ ] 添加音频反馈
- [ ] 整合钓鱼技能到玩家属性系统
- [ ] 添加触控支持

---

## 六、测试建议

### 6.1 功能测试

1. 按 F 键触发钓鱼系统
2. 按 G 键打开观云镜
3. 按 Y 键打开契约面板
4. 在观云镜中按 ESC 关闭
5. 在契约面板中领取奖励
6. 完成钓鱼小游戏并验证物品添加

### 6.2 边界测试

1. 多个UI同时打开时的优先级
2. 日切后观云镜内容刷新
3. 存档加载后系统状态恢复

---

**文档版本**: v1.0
**创建日期**: 2026-05-02
